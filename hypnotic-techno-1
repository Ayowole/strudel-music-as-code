// HYPNO.TECHNO 140 BPM 4 Blocks: Intro → Grow → Chorus → Outro

 setcps(140/60/4)

// GLOBAL SCALE / ROOT
const root = "c2"          // change root note here
const scl  = "minor"       // scale: try "phrygian", "aeolian"


// BLOCK 1 — INTRO: Sparse kick + sub pulse. Cold open.

//  $: stack(

//    // --- KICK — four on the floor ---
//   sound("bd*4")
//     .gain(0.9)
//     .shape(0.3)           // waveshaper distortion 0-1
//     .room(0.1)            // reverb size (dry→wet)
//     .lpf(200),            // low-pass cutoff Hz — muffle highs

//   // --- SUB BASS PULSE — slow throb ---
// note("c1 ~ ~ c1")
//   .s("sawtooth")
//   .gain(0.5)
//   .lpf(sine.range(60, 120).slow(8))  // LFO on filter cutoff
//   .attack(0.2).release(1.5)  // slow attack, long release
//   .room(0.4),

//   // --- SPARSE HI-HAT — every 4 bars on upbeat ---
//   sound("~ hh ~ ~")
//     .gain(0.35)
//     .hpf(8000)               // high-pass — let only air through
//     .pan(rand)               // random stereo position each hit
//     .delay(0.3).delaytime(0.375) // dotted-8th delay

// )

// BLOCK 2 — GROW: Layers build. Filter opens slowly.

// $: stack(

//   // --- KICK — same, harder ---
//   sound("bd*4")
//     .gain(0.95)
//     .shape(0.5)            // more distortion in the grow
//     .room(0.08),

//   // --- ACIDIC 303 BASSLINE — filter sweeps up ---
//   note("c2 eb2 g1 bb1 c2 g1 f2 eb2")
//     .s("sawtooth")
//     .gain(0.7)
//     .lpf(sine.range(300, 2000).slow(16))  // slow filter sweep over 16 bars
//     .resonance(18)          // filter resonance — acidic peak
//     .attack(0.01).decay(0.15).sustain(0.4).release(0.2)
//     .crush(6),              // bit-crush depth (1=heavy, 8=subtle)

//   // --- CLAP — on 2 and 4 ---
//   sound("~ cp ~ cp")
//     .gain(sine.range(0.3, 0.7).slow(8))  // gain swells
//     .room(0.5).roomsize(0.8)
//     .hpf(2000),

//   // --- OFFBEAT CLOSED HH — groove pulse ---
//   sound("hh(3,8)")
//     .gain(0.4)
//     .hpf(7000)
//     .pan(sine.range(0.2, 0.8).slow(4))  // slow autopan
//     .speed(rand.range(0.9, 1.1)),         // micro pitch random

//   // --- CHORD STAB — short hit on beat 3 ---
//   note("~ ~ [c3,eb3,g3] ~")
//     .s("square")
//     .gain(0.25)
//     .lpf(800)
//     .attack(0.005).release(0.3)
//     .room(0.6)
//     .chorus(0.4)            // chorus depth 0-1

// )


// BLOCK 3 — CHORUS: Full energy. All layers. Peak hypnosis.

// $: stack(

//   // --- KICK — maximum punch ---
//   sound("bd*4")
//     .gain(1)
//     .shape(0.6)
//     .lpf(180)
//     .room(0.05),

//   // --- FULL ACID BASSLINE ---
//   note("c2 eb2 g1 bb1 c2 g1 f2 eb2 d2 ~ g1 bb1 c2 c2 eb2 ~")
//     .s("sawtooth")
//     .gain(0.75)
//     .lpf(sine.range(500, 3500).slow(8))
//     .resonance(22)           // high resonance = screaming 303
//     .attack(0.005).decay(0.12).sustain(0.3).release(0.1)
//     .distort(0.4)            // soft-clip distortion
//     .crush(8),               // lighter crush for texture

//   // --- CLAP + SNARE LAYER ---
//   sound("~ [cp,sd] ~ [cp,sd]")
//     .gain(0.8)
//     .room(0.35).roomsize(0.7),

//   // --- OPEN HH ROLLS — tension builds ---
//   sound("oh(5,8)")
//     .gain(0.3)
//     .hpf(6000)
//     .pan(rand),

//   // --- HYPNOTIC LEAD — short arpeggiated melody ---
//   note("c4 eb4 g4 bb4 g4 f4 eb4 d4")
//     .s("triangle")
//     .gain(sine.range(0.2, 0.5).slow(4))  // breathing gain
//     .lpf(sine.range(1000, 4000).slow(4))
//     .attack(0.01).release(0.2)
//     .delay(0.5).delaytime(0.25).delayfeedback(0.4)  // ping-pong delay
//     .room(0.7).roomsize(0.9),

//   // --- PERCUSSION — metallic texture ---
//   sound("metal(7,16)")
//     .gain(0.2)
//     .hpf(4000)
//     .speed(rand.range(0.5, 2))  // random pitch each hit
//     .pan(rand),

//   // --- PAD — tonal cloud underneath ---
//   note("[c3,g3,eb4]")
//     .s("pad")
//     .gain(0.15)
//     .lpf(900)
//     .attack(2).release(3)    // very slow envelope — sustained wash
//     .room(0.9).roomsize(0.95)  // cavernous reverb
//     .chorus(0.6)

// )

// BLOCK 4 — OUTRO: Elements drop away. Filter closes.

// $: stack(

//   // --- KICK — diminishing, every 8 bars remove ---
//   sound("bd*4")
//     .gain(sine.range(0.3, 0.9).slow(16))  // gain fades over 16 bars
//     .lpf(150)                // muffle progressively
//     .room(0.5),

//   // --- BASS — same melody, filter CLOSES ---
//   note("c2 eb2 g1 bb1")
//     .s("sawtooth")
//     .gain(sine.range(0, 0.6).slow(16))   // fade out over 16 bars
//     .lpf(sine.range(80, 600).slow(16))   // filter sweep DOWN
//     .resonance(12)
//     .attack(0.3).release(2),

//   // --- PAD — solo last element, long decay ---
//   note("[c2,g2]")
//     .s("pad")
//     .gain(0.2)
//     .attack(4).release(8)    // very long envelope
//     .lpf(400)
//     .room(0.98).roomsize(0.99)  // almost full wet reverb tail
//     .delay(0.8).delaytime(0.5).delayfeedback(0.6)  // deep echo fadeout

// )
